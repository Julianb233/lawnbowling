---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/components/ui/button.tsx
  - src/lib/utils.ts
  - src/lib/types.ts
  - next.config.ts
  - tsconfig.json
  - .env.local.example
  - public/manifest.json
autonomous: true

must_haves:
  truths:
    - "App builds without errors via `npm run build`"
    - "App runs locally via `npm run dev` without crashing"
    - "Tailwind CSS 4 styles render correctly (dark theme, emerald accents)"
    - "Radix UI primitives are importable and functional"
    - "PWA manifest is valid and references correct icons"
  artifacts:
    - path: "package.json"
      provides: "All dependencies installed and resolvable"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, metadata, viewport"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 import, CSS custom properties, PWA-safe-area styles"
    - path: "next.config.ts"
      provides: "Serwist PWA config, turbopack settings"
    - path: "tsconfig.json"
      provides: "TypeScript config with @/ path alias"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "next.config.ts"
      to: "src/app/sw.ts"
      via: "Serwist swSrc config"
      pattern: "swSrc.*sw\\.ts"
---

<objective>
Verify and fix the existing Next.js 16 scaffold so that `npm install`, `npm run build`, and `npm run dev` all succeed. The project already has 139 source files scaffolded -- this plan audits and repairs the foundation rather than creating from scratch.

Purpose: Ensure the project compiles and runs before wiring auth and database in Plan 02.
Output: A clean-building Next.js 16 app with Tailwind CSS 4, Radix UI, and Serwist PWA configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@package.json
@next.config.ts
@src/app/layout.tsx
@src/app/globals.css
@src/lib/types.ts
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix build errors in the existing scaffold</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    src/app/layout.tsx
    src/app/globals.css
    src/lib/utils.ts
    src/components/ui/button.tsx
    public/manifest.json
    .env.local.example
  </files>
  <action>
    1. Run `npm install` in the project root. Fix any dependency resolution errors.

    2. Run `npm run build` and collect ALL errors. The scaffold has 139 files -- many will reference
       Supabase tables/queries that don't exist yet. The goal is to make the build succeed, NOT to
       delete scaffolded files.

    3. For files that import from Supabase and fail because there's no database connection:
       - Add `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `.env.local` with
         placeholder values if not already set. Check 1Password for "pick-a-partner" or "Supabase"
         entries first: `op item list | grep -i pick` and `op item list | grep -i supabase`.
       - If real Supabase credentials exist, use them. If not, the build should still succeed with
         the env vars set (Supabase client creation doesn't fail at build time, only at runtime).

    4. Fix TypeScript errors one by one. Common issues in scaffolded code:
       - Missing imports (fix with correct paths)
       - Type mismatches between `src/lib/types.ts` and actual Supabase schema columns
         (e.g., `players` table uses `display_name` in schema.sql but `name` in types.ts --
         the types.ts `Player` interface has `name` but schema.sql has `display_name`. Align
         types.ts to match schema.sql since schema.sql is the source of truth)
       - The `player.id` vs `user_id` mismatch: In `page.tsx` and `auth/callback/route.ts`,
         player is queried with `.eq("id", user.id)` but schema.sql uses `id` as its own UUID
         and `user_id` as the FK to auth.users. Fix queries to use `.eq("user_id", user.id)`.
       - Server components using client-only hooks (add "use client" or restructure)

    5. Ensure `public/manifest.json` exists with valid PWA manifest:
       ```json
       {
         "name": "Pick a Partner",
         "short_name": "Partner",
         "description": "Find your sports partner at the venue",
         "start_url": "/",
         "display": "standalone",
         "background_color": "#09090b",
         "theme_color": "#22c55e",
         "icons": [
           { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
           { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
         ]
       }
       ```
       Create placeholder icon PNGs in `public/icons/` if missing (simple green circle SVG converted or 1x1 placeholder).

    6. Verify `.env.local.example` includes all needed env vars:
       ```
       NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
       SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
       ```

    7. Run `npm run build` again. It MUST succeed with 0 errors. If there are stubbed pages that
       import complex components which fail, add minimal stub exports to those components rather
       than deleting the files. The scaffold is intentional -- preserve all 139 files.
  </action>
  <verify>
    - `npm run build` exits with code 0
    - `npm run dev` starts without crashing (check first 5 seconds of output)
    - `npx tsc --noEmit` passes with 0 errors
  </verify>
  <done>
    The project builds successfully. All 139 source files compile. TypeScript reports 0 errors.
    Dev server starts without crashing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Tailwind CSS 4, Radix UI, and PWA configuration</name>
  <files>
    src/app/globals.css
    src/components/ui/button.tsx
    src/app/sw.ts
    next.config.ts
  </files>
  <action>
    1. Confirm Tailwind CSS 4 is working:
       - `src/app/globals.css` should start with `@import "tailwindcss";` (Tailwind v4 syntax)
       - Verify `@tailwindcss/postcss` is in devDependencies and `postcss.config.mjs` exists
         with `export default { plugins: { "@tailwindcss/postcss": {} } }`
       - If postcss config is missing, create it

    2. Confirm Radix UI primitives work:
       - Check that all `@radix-ui/*` packages in package.json resolve correctly
       - Do a quick smoke test: in the dev server, the login page should render the Button component
         without errors

    3. Confirm Serwist PWA setup:
       - `src/app/sw.ts` should exist (it does) with a valid Serwist service worker setup
       - `next.config.ts` should use `withSerwistInit` (it does)
       - Verify the `disable: process.env.NODE_ENV === "development"` flag is set so dev mode
         doesn't try to register the service worker

    4. If any of the above are broken, fix them. Do NOT change the overall architecture -- just
       make what exists work.
  </action>
  <verify>
    - `postcss.config.mjs` exists with Tailwind v4 postcss plugin
    - Button component renders with emerald-600 background (check page source or dev tools)
    - `src/app/sw.ts` exists and is referenced in next.config.ts
    - Build still passes after any changes
  </verify>
  <done>
    Tailwind CSS 4 generates utility classes. Radix UI components import without errors.
    Serwist is configured but disabled in dev mode. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `npm run dev` starts and serves pages
3. `npx tsc --noEmit` reports 0 errors
4. Visit `http://localhost:3000/login` -- page renders with styled form
5. `public/manifest.json` is valid JSON with PWA fields
</verification>

<success_criteria>
- Zero build errors
- Zero TypeScript errors
- Dev server starts and serves the login page
- All 139 source files preserved (none deleted)
- Tailwind, Radix, Serwist all functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
