# Plan 01-01: Next.js Project Scaffold, Supabase Setup, Tailwind + Radix UI Config

## Requirements Covered
- AUTH-01 (partial): Project infrastructure for Supabase Auth
- AUTH-02 (partial): Session persistence infrastructure

## Context

This plan covers the project scaffold, dependencies, Supabase client setup, Tailwind CSS + Radix UI configuration, PWA manifest, and base layout.

## Existing Code Assessment

**ALREADY BUILT** — This plan is ~95% complete. The following exist and are functional:

| Component | File(s) | Status |
|-----------|---------|--------|
| Next.js 16 App Router | `package.json`, `src/app/layout.tsx` | Done |
| Tailwind CSS v4 | `postcss.config.mjs`, `src/app/globals.css` | Done |
| Radix UI primitives | `@radix-ui/react-*` in package.json (avatar, checkbox, dialog, dropdown, select, tabs, toast, toggle-group) | Done |
| Supabase browser client | `src/lib/supabase/client.ts` | Done |
| Supabase server client | `src/lib/supabase/server.ts` | Done |
| Supabase middleware | `src/lib/supabase/middleware.ts` + `src/middleware.ts` | Done |
| PWA manifest + install prompt | `public/manifest.json`, `src/components/pwa/InstallPrompt.tsx` | Done |
| Serwist service worker | `src/app/sw.ts`, `@serwist/next` in deps | Done |
| UI button component | `src/components/ui/button.tsx` | Done |
| Page transitions | `src/components/ui/PageTransition.tsx` | Done |
| Skeleton loader | `src/components/ui/Skeleton.tsx` | Done |
| TypeScript types | `src/lib/types.ts` (all domain types) | Done |
| Design tokens | `src/lib/design.ts` | Done |
| Utility functions | `src/lib/utils.ts` (clsx + tailwind-merge) | Done |
| Framer Motion | In deps for animations | Done |
| Lucide icons | In deps | Done |
| Dark theme | `html.dark` class + zinc-950 bg | Done |

## Remaining Gaps

1. **Environment variables validation** — No runtime check that `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` are set. App will crash with unhelpful error if missing.
2. **`.env.example`** — No `.env.example` file documenting required env vars for developer onboarding.
3. **Build verification** — Need to confirm `next build` succeeds without errors.

## Steps

1. Create `.env.example` with required Supabase env vars
2. Add env var validation helper (optional, low priority — can skip if build works)
3. Verify `next build` succeeds (the true acceptance test for this plan)

## Acceptance Criteria

- [x] Next.js 16 App Router project exists with Tailwind + Radix UI
- [x] Supabase client/server/middleware utilities configured
- [x] PWA manifest and service worker configured
- [x] Dark theme applied globally
- [ ] `.env.example` documents required env vars
- [ ] `next build` succeeds without errors

## Estimated Effort
Small — mostly verification. ~15 minutes of new work.

## Dependencies
None (first plan)
