---
phase: 03-live-availability-board
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/board/page.tsx
  - src/components/board/AvailabilityBoard.tsx
  - src/components/board/BoardFilters.tsx
  - src/components/board/LiveIndicator.tsx
  - src/components/board/PlayerCard.tsx
autonomous: false

must_haves:
  truths:
    - "Board displays player cards in a responsive grid: 1 col on iPhone portrait, 2 cols on iPad portrait, 3-4 cols on iPad landscape"
    - "Each player card shows name, avatar (or initials fallback), skill level stars, sport tags, and check-in time"
    - "Sport filter buttons toggle to show only players of that sport; skill dropdown filters by level"
    - "Filters can be combined (sport AND skill shows only matching players)"
    - "Board updates live when a new player checks in or an existing player checks out"
    - "LiveIndicator shows green LIVE when connected, amber CONNECTING on initial load"
  artifacts:
    - path: "src/app/board/page.tsx"
      provides: "Board page wiring filters, check-in, realtime hook, and grid together"
      contains: "useRealtimePlayers"
    - path: "src/components/board/AvailabilityBoard.tsx"
      provides: "Responsive grid of PlayerCard components with empty state"
      contains: "grid-cols"
    - path: "src/components/board/PlayerCard.tsx"
      provides: "Individual player card with avatar, name, skill stars, sport tags, check-in time"
      contains: "checked_in_at"
    - path: "src/components/board/BoardFilters.tsx"
      provides: "Sport toggle buttons and skill level dropdown with responsive stacking"
      contains: "onSportChange"
    - path: "src/components/board/LiveIndicator.tsx"
      provides: "Live badge with player count and connection status"
      contains: "connected"
  key_links:
    - from: "src/app/board/page.tsx"
      to: "src/lib/hooks/useRealtimePlayers.ts"
      via: "useRealtimePlayers hook call with connected state"
      pattern: "connected.*useRealtimePlayers"
    - from: "src/app/board/page.tsx"
      to: "src/components/board/LiveIndicator.tsx"
      via: "passing connected prop"
      pattern: "LiveIndicator.*connected"
---

<objective>
Polish the board UI for responsive iPad landscape + iPhone portrait layouts, wire connection status into the LiveIndicator, ensure touch targets meet 44px minimum, and verify the complete board satisfies all AVAIL requirements end-to-end.

Purpose: The board components exist and work but need responsive layout tuning for the two primary device targets (iPad landscape kiosk at 1024px+ and iPhone portrait at 375px), connection status display, and touch target compliance for PWA readiness.

Output: A polished, responsive availability board showing live connection status, with proper responsive stacking on mobile and touch-friendly controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-availability-board/03-01-SUMMARY.md

Key existing files:
@src/app/board/page.tsx - Full board page with filters, check-in, sidebar, partner request modal
@src/components/board/AvailabilityBoard.tsx - Grid rendering with loading/empty states
@src/components/board/PlayerCard.tsx - Card with avatar, skill stars, sport tags, check-in time, PICK ME button
@src/components/board/BoardFilters.tsx - Sport toggles + skill dropdown
@src/components/board/LiveIndicator.tsx - LIVE badge with player count (no connection status yet)
@src/components/board/BottomNav.tsx - Mobile bottom nav with safe-area padding
@src/lib/types.ts - Player, Sport, SkillLevel types and constants
@src/lib/design.ts - Sport color mappings (primary, glow, gradient, bg, ring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive layout polish and LiveIndicator connection status</name>
  <files>
    src/components/board/AvailabilityBoard.tsx
    src/components/board/BoardFilters.tsx
    src/components/board/LiveIndicator.tsx
    src/components/board/PlayerCard.tsx
    src/app/board/page.tsx
  </files>
  <action>
1. **AvailabilityBoard.tsx** -- Refine grid breakpoints for iPad/iPhone:
   - Change grid classes from `grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
     to `grid gap-3 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-4 lg:gap-4`
   - This maps to: iPhone (1 col), iPad portrait (2 cols at 640px+), iPad landscape (3 cols at 768px+), wide screens (4 cols at 1280px+)
   - Apply same grid classes to the skeleton loading state grid

2. **BoardFilters.tsx** -- Stack filters on mobile:
   - Change outer container from `flex flex-wrap items-center gap-3` to `flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-center`
   - Change skill dropdown wrapper from `ml-auto` to `sm:ml-auto w-full sm:w-auto`
   - The select element should be `w-full sm:w-auto` on mobile for full-width tap target
   - Add `min-h-[44px]` to each sport filter button for touch compliance
   - Add `min-h-[44px]` to the skill select dropdown

3. **LiveIndicator.tsx** -- Add connection status prop:
   - Update interface to: `{ count: number; connected?: boolean }`
   - When `connected` is false or undefined: show amber dot (change `bg-emerald-400` to `bg-amber-400`, same for ping animation), text says "CONNECTING" instead of "LIVE"
   - When `connected` is true: show existing green "LIVE" indicator (no changes to current behavior)
   - Keep the AnimatePresence player count animation unchanged

4. **PlayerCard.tsx** -- Touch target compliance:
   - Add `min-h-[44px]` to the "PICK ME" button (it already has py-2.5 which is ~40px, this ensures compliance)

5. **board/page.tsx** -- Wire connected state:
   - Destructure `connected` from useRealtimePlayers: `const { players, loading, connected } = useRealtimePlayers({ sportFilter, skillFilter })`
   - Pass to LiveIndicator: `<LiveIndicator count={players.length} connected={connected} />`
   - No other changes needed -- the filter wiring, check-in button, and sidebar are already correct
  </action>
  <verify>
    Run `npx next build` to confirm no TypeScript errors. Grep AvailabilityBoard.tsx for "md:grid-cols-3". Grep BoardFilters.tsx for "flex-col". Grep LiveIndicator.tsx for "connected". Grep board/page.tsx for "connected".
  </verify>
  <done>
    Board grid shows 1 col on iPhone, 2 on small tablet, 3 on iPad landscape, 4 on wide screens. Filters stack vertically on mobile. LiveIndicator shows CONNECTING (amber) or LIVE (green) based on realtime connection. All interactive elements have 44px minimum touch targets. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 availability board with:
    - Server-side check-in/out API route with auth validation (Plan 01)
    - Supabase Realtime subscription with connection status tracking (Plan 01)
    - Responsive grid layout: 1 col iPhone portrait, 2-3 col iPad, 4 col wide (Plan 02)
    - Vertically stacked filters on mobile, horizontal on tablet+ (Plan 02)
    - Player cards with avatar, name, skill stars, sport tags, check-in time (existing)
    - Combined sport + skill filtering via AND logic (existing)
    - LIVE/CONNECTING connection indicator (Plan 02)
    - 44px touch targets on buttons and controls (Plan 02)
    - Documented RLS policies and Realtime publication in schema.sql (Plan 01)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/board
    2. Verify the board renders (may show empty state "No players available" if no seeded data)
    3. Check the LIVE indicator in the header -- should show green "LIVE" when connected or amber "CONNECTING" briefly
    4. Open DevTools, resize to iPhone width (375px):
       - Verify single-column card grid
       - Verify filters stack vertically (sport buttons on top, skill dropdown full-width below)
       - Verify bottom nav appears
    5. Resize to iPad landscape (1024px):
       - Verify 3-column grid
       - Verify filters are horizontal
       - Verify sidebar appears with Match Queue and Courts Status
       - Verify bottom nav is hidden
    6. If authenticated: tap the check-in button, verify it toggles between "I'M AVAILABLE" (green) and "CHECK OUT" (red)
    7. Open a second browser tab to /board -- when you toggle check-in in tab 1, verify the player appears/disappears on tab 2 without refresh
    8. Test sport filter: tap Pickleball button -- should highlight green, other sport players should disappear
    9. Test combined filter: select Pickleball + Advanced skill -- only advanced pickleball players shown
    10. Tap a sport filter again to deselect -- all players return
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` passes with no errors
2. Board page renders with responsive grid (1/2/3/4 columns at breakpoints)
3. LiveIndicator shows LIVE (green) when realtime connected, CONNECTING (amber) when not
4. BoardFilters stacks vertically on iPhone portrait, horizontal on iPad+
5. PlayerCard PICK ME button has min-h-[44px] touch target
6. board/page.tsx passes `connected` from useRealtimePlayers to LiveIndicator

Full Phase 3 requirements coverage:
- AVAIL-01: Check-in marks player as available (CheckInButton -> /api/checkin)
- AVAIL-02: Check-out removes from board (CheckInButton toggle + Realtime propagation)
- AVAIL-03: Live board via Supabase Realtime (useRealtimePlayers with connection tracking)
- AVAIL-04: Filterable by sport and skill (BoardFilters + useRealtimePlayers AND logic)
- AVAIL-05: Check-in time displayed (PlayerCard timeAgo function)
</verification>

<success_criteria>
- Responsive layout works on iPhone portrait (375px) and iPad landscape (1024px+)
- Player cards display all required fields (name, avatar, skill stars, sports, check-in time)
- Sport and skill filters work independently and combined (AND logic)
- LiveIndicator shows real-time connection status
- Board updates in real-time across multiple browser tabs when players check in/out
- All touch targets are 44px minimum
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-availability-board/03-02-SUMMARY.md`
</output>
