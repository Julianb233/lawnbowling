# 07-01: Find-and-Replace Rebrand, Logo Integration, Color Scheme, Manifest Update

## Status: COMPLETED

## Summary
Rebranded all remaining "Pick a Partner" / "pick-a-partner" references to "Lawnbowling" / "lawnbowling" across the codebase. Updated color scheme verification, manifest validation, and meta tags confirmation.

## Changes Made

### Source Code (.ts/.tsx)
1. `playwright.config.ts` — Changed BASE_URL from `pick-a-partner.vercel.app` to `lawnbowl.app`
2. `src/components/insurance/InsuranceOffer.tsx` — Updated DEI URL from `/m/pick-a-partner/` to `/m/lawnbowling/`
3. `src/app/insurance/InsurancePage.tsx` — Updated 3 DEI URLs from `/m/pick-a-partner/` to `/m/lawnbowling/`
4. `src/app/insurance/lawn-bowls/LawnBowlsInsurancePage.tsx` — Updated 6 DEI URLs from `/m/pick-a-partner/` to `/m/lawnbowling/`
5. `src/app/for-venues/page.tsx` — Updated 2 DEI URLs from `/m/pick-a-partner/` to `/m/lawnbowling/`

### Documentation (.md)
6. `docs/MOCKUPS.md` — Title and 4 UI mockup brand name strings
7. `docs/PROPOSAL.md` — Document title and executive summary brand name
8. `docs/screenshots/README.md` — Title, URLs, and brand references

### Config
9. `.planning/bussit/config.json` — Project name and path

## Already Rebranded (no changes needed)
- `src/app/layout.tsx` — Already uses "Lawnbowling" throughout
- `public/manifest.json` — Already uses "Lawnbowling" name, #1B5E20 theme color
- `package.json` — Already named "lawnbowling"
- `src/lib/schema.ts` — Already uses "Lawnbowling" in all structured data
- `globals.css` — Kiosk palette already uses #1B5E20 as primary

## Color Scheme Verification
- Primary brand green: #1B5E20 (manifest theme_color, viewport themeColor, kiosk primary)
- Background: #f0fdf4 (manifest background_color)
- Tailwind emerald utilities used throughout components (appropriate for lawn bowling)
- No color changes were needed — scheme was already correct

## Verification
- `npx tsc --noEmit` passed with zero errors
- `npm run build` could not complete due to concurrent build processes from other agents sharing the .next directory, but TypeScript compilation confirms all changes are syntactically valid
