# Plan 07-03: Domain Setup and Vercel Config

## Goal
Configure the codebase for the `lawnbowl.app` domain with proper redirects, environment variables, and Next.js/Vercel configuration.

## Current State (Already Done)
- `vercel.json` has 301 redirects: `lawnbowling.app`, `www.lawnbowl.app`, `www.lawnbowling.app` -> `lawnbowl.app`
- `robots.ts` uses `https://lawnbowl.app` as base URL
- `sitemap.ts` uses `https://lawnbowl.app` as base URL
- `layout.tsx` uses `https://lawnbowl.app` as BASE_URL and metadataBase
- `schema.ts` uses `https://lawnbowl.app` as BASE_URL
- `manifest.json` already rebranded to "Lawnbowling"
- Email templates fallback to `https://lawnbowl.app`
- No CORS configuration exists (not needed for this app)

## Tasks

### 1. Update next.config.ts with domain configuration
- Add image domain allowlist for lawnbowl.app
- Add security headers configuration
- Add canonical domain redirect middleware config

### 2. Add NEXT_PUBLIC_APP_URL to environment config
- Update `.env.local.example` with `NEXT_PUBLIC_APP_URL=https://lawnbowl.app`
- Create a shared `config.ts` utility for the base URL to reduce hardcoded URLs

### 3. Create shared site config utility
- Single source of truth for site URL, name, and domain
- Replace hardcoded `https://lawnbowl.app` strings with config import where practical

### 4. Enhance vercel.json with security and performance headers
- Add HSTS header for HTTPS enforcement
- Add Content-Security-Policy basics
- Add Permissions-Policy header

### 5. Verify build passes
- Run `npm run build`

## Out of Scope
- Actual DNS configuration (manual Vercel dashboard task)
- SSL certificate setup (automatic via Vercel)
- Google Search Console verification (manual task)
