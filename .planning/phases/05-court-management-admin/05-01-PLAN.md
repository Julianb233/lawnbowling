---
phase: 05-court-management-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/db/venues.ts
  - src/app/api/admin/venue/route.ts
  - src/app/admin/venue/page.tsx
  - src/app/admin/courts/page.tsx
  - src/lib/db/courts.ts
  - src/app/api/admin/courts/route.ts
  - src/components/courts/CourtStatusBoard.tsx
  - src/components/courts/CourtCard.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can add and remove sports available at the venue from the venue settings page"
    - "Court CRUD sport dropdown uses the venue's configured sports list instead of the hardcoded array"
    - "Admin can force-end a match and free its court from the courts admin page"
    - "Court status board on /board updates in real-time when court/match state changes"
    - "Completing a match via End Match button frees the court and auto-assigns the next queued pair"
  artifacts:
    - path: "src/lib/db/venues.ts"
      provides: "getVenueSports and updateVenueSports functions"
      contains: "updateVenueSports"
    - path: "src/app/admin/venue/page.tsx"
      provides: "Sports management UI section with add/remove chips"
      contains: "Sports Available"
    - path: "src/app/admin/courts/page.tsx"
      provides: "Court list with active match context and Force End button for occupied courts"
      contains: "Force End"
    - path: "src/app/api/admin/courts/route.ts"
      provides: "PATCH endpoint for force-ending a court's active match"
      exports: ["GET", "POST", "PUT", "DELETE", "PATCH"]
  key_links:
    - from: "src/app/admin/venue/page.tsx"
      to: "/api/admin/venue"
      via: "PATCH fetch for sports array update"
      pattern: "PATCH.*api/admin/venue"
    - from: "src/app/admin/courts/page.tsx"
      to: "/api/admin/courts"
      via: "PATCH fetch for force-end"
      pattern: "PATCH.*api/admin/courts"
    - from: "src/app/admin/courts/page.tsx"
      to: "/api/admin/venue"
      via: "GET fetch to load venue sports for dropdown"
      pattern: "fetch.*api/admin/venue"
---

<objective>
Close remaining Phase 5 gaps: venue sports management (ADMIN-03), court force-end/unassign (ADMIN-05 enhancement), and dynamic sports in court CRUD.

Purpose: Research confirmed MATCH-02 through MATCH-05 are fully implemented. Two gaps remain: (1) ADMIN-03 -- no UI for managing venue sports (hardcoded array in courts page), and (2) admin can't force-end an active match from the courts management page. The DB schema already has `venues.sports text[]`.

Output: Working sports management on venue page, dynamic sports dropdown on courts page, force-end capability on courts page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-court-management-admin/05-RESEARCH.md

Key existing files (read before modifying):
@src/lib/types.ts
@src/lib/db/venues.ts
@src/lib/db/courts.ts
@src/app/api/admin/venue/route.ts
@src/app/api/admin/courts/route.ts
@src/app/admin/venue/page.tsx
@src/app/admin/courts/page.tsx
@src/components/courts/CourtStatusBoard.tsx
@src/components/courts/CourtCard.tsx
@src/lib/auth/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Venue sports management (ADMIN-03)</name>
  <files>
    src/lib/types.ts
    src/lib/db/venues.ts
    src/app/api/admin/venue/route.ts
    src/app/admin/venue/page.tsx
    src/app/admin/courts/page.tsx
  </files>
  <action>
    The DB column `venues.sports text[]` already exists. Wire up admin control:

    1. **src/lib/types.ts** -- Add `sports: string[]` to the `Venue` interface (between `timezone` and `created_at`).

    2. **src/lib/db/venues.ts** -- Add two functions:
       - `getVenueSports(venueId?: string): Promise<string[]>` -- calls `getVenue(venueId)`, returns `venue.sports ?? ["pickleball", "lawn_bowling", "tennis"]` (fallback to defaults if null/empty).
       - `updateVenueSports(venueId: string, sports: string[]): Promise<Venue>` -- updates venue row with `{ sports }` via supabase `.update({ sports }).eq("id", venueId).select().single()`, returns updated venue.

    3. **src/app/api/admin/venue/route.ts** -- Add a `PATCH` export handler:
       - Same admin auth check pattern as existing GET/PUT (create supabase client, getUser, isAdmin check).
       - Parse body: `{ id: string, sports: string[] }`.
       - Validate: `id` required, `sports` must be a non-empty array of strings, each sport lowercase alphanumeric + underscores only.
       - Call `updateVenueSports(id, sports)`.
       - Return `NextResponse.json({ venue })`.

    4. **src/app/admin/venue/page.tsx** -- Add "Sports Available" section below the timezone input:
       - Fetch venue on mount (existing `useEffect` already does this -- `venue.sports` will now be available).
       - Display current sports as a row of removable tag chips. Each chip: `<span className="inline-flex items-center gap-1 rounded-full bg-emerald-500/20 px-3 py-1 text-sm text-emerald-400">` with sport name (capitalized, underscores replaced with spaces) and an X button to remove.
       - Below chips, add an input + "Add Sport" button row (same styling as existing inputs). On add, lowercase the input, replace spaces with underscores, push to sports array.
       - Include quick-add buttons for common sports not in the list: pickleball, lawn_bowling, tennis, badminton, table_tennis, volleyball. Each is a small outline button that only shows if that sport is NOT already in the list.
       - "Save Sports" button (separate from the existing "Save Changes" for name/address/timezone). Calls `PATCH /api/admin/venue` with `{ id: venue.id, sports: sportsArray }`.
       - Show success/error message after save (same pattern as existing `message` state -- can use a separate `sportsMessage` state).
       - Store sports in local state: `const [sports, setSports] = useState<string[]>([])`. Initialize from `venue.sports` when venue loads (in the existing `.then` callback, add `setSports(data.venue?.sports ?? [])`).

    5. **src/app/admin/courts/page.tsx** -- Replace hardcoded SPORTS array:
       - Add state: `const [availableSports, setAvailableSports] = useState<string[]>(SPORTS)` (keep SPORTS as fallback).
       - In the existing `useEffect`, add a second fetch: `fetch("/api/admin/venue").then(r => r.json()).then(data => { if (data.venue?.sports?.length) setAvailableSports(data.venue.sports); })`.
       - Replace all references to `SPORTS` in JSX (the two `<select>` elements for add and edit) with `availableSports`.
       - Keep the `SPORTS` const as a fallback default at the top of the file.

    Sport format convention: lowercase with underscores (e.g. "lawn_bowling"). Display format: `.replace(/_/g, " ")` then capitalize first letter of each word. This pattern is already used throughout the codebase.
  </action>
  <verify>
    - `npm run build` passes
    - Visit /admin/venue -- see "Sports Available" section with chips and quick-add buttons
    - Add "volleyball" sport, save, refresh -- volleyball persists
    - Remove a sport, save, refresh -- sport is gone
    - Visit /admin/courts -- sport dropdown matches venue's configured sports
  </verify>
  <done>
    Admin can add/remove sports at the venue (ADMIN-03). Court CRUD uses dynamic sports from venue config. DB schema's venues.sports column is wired to the UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin court force-end and realtime hardening</name>
  <files>
    src/lib/db/courts.ts
    src/app/api/admin/courts/route.ts
    src/app/admin/courts/page.tsx
    src/components/courts/CourtStatusBoard.tsx
    src/components/courts/CourtCard.tsx
  </files>
  <action>
    Add force-end capability for admin and harden realtime subscriptions:

    1. **src/lib/db/courts.ts** -- Add `forceEndMatch(courtId: string)` function:
       - Query matches table for record with `court_id = courtId` AND `status = 'playing'`.
       - If found: update match to `{ status: 'completed', ended_at: new Date().toISOString() }`.
       - Get match_players for that match via `supabase.from("match_players").select("player_id").eq("match_id", matchData.id)`.
       - Collect player_ids from result.
       - Update those players: set `is_available: true` via `.update({ is_available: true }).in("id", playerIds)`.
       - Update court: set `is_available: true` via `.update({ is_available: true }).eq("id", courtId)`.
       - Do NOT auto-assign the next queued match (admin is manually clearing -- they may want the court empty).
       - If no active match found, just ensure court is marked `is_available: true`.
       - Return void.

    2. **src/app/api/admin/courts/route.ts** -- Add `PATCH` export handler:
       - Admin auth check using the existing `requireAdminUser()` function at the top of the file.
       - Parse body: `{ courtId: string, action: "force_end" }`.
       - Validate: `courtId` required, `action` must be `"force_end"`.
       - Call `forceEndMatch(courtId)` (import from `@/lib/db/courts`).
       - Return `NextResponse.json({ success: true })`.

    3. **src/app/admin/courts/page.tsx** -- Enhance to show active match context:
       - Import `createClient` from `@/lib/supabase/client`.
       - Add state: `const [activeMatches, setActiveMatches] = useState<Record<string, { matchId: string; players: string[] }>>({})`.
       - In the existing `fetchCourts` function, after fetching courts, also fetch active matches:
         ```typescript
         const supabase = createClient();
         const { data: matches } = await supabase
           .from("matches")
           .select("id, court_id, match_players(player_id, players(display_name))")
           .eq("status", "playing")
           .not("court_id", "is", null);
         ```
       - Build a lookup object: `{ [courtId]: { matchId, players: [displayName1, displayName2] } }` and set it in state.
       - In the court list rendering, for each court where `!court.is_available` and `activeMatches[court.id]` exists:
         - Below the court name/sport, show: `<p className="text-xs text-zinc-400 mt-1">Playing: {activeMatches[court.id].players.join(" vs ")}</p>`
         - Add a "Force End" button next to Edit/Delete: `<Button size="sm" variant="destructive" onClick={() => handleForceEnd(court.id)}>Force End</Button>`
       - Add `handleForceEnd` function:
         ```typescript
         const handleForceEnd = async (courtId: string) => {
           if (!confirm("Force end this match and free the court?")) return;
           await fetch("/api/admin/courts", {
             method: "PATCH",
             headers: { "Content-Type": "application/json" },
             body: JSON.stringify({ courtId, action: "force_end" }),
           });
           fetchCourts();
         };
         ```

    4. **src/components/courts/CourtStatusBoard.tsx** -- Harden realtime:
       - In the `useEffect` subscription, add a third `.on()` for `match_players` table:
         ```typescript
         .on("postgres_changes", { event: "*", schema: "public", table: "match_players" }, () => fetchData())
         ```
       - In `fetchData`, add defensive null handling for matchesRes: `setMatches((matchesRes.data ?? []).map(m => ({ ...m, match_players: (m as any).match_players ?? [] })) as ActiveMatch[])`.

    5. **src/components/courts/CourtCard.tsx** -- Add null safety:
       - In the playing section: change `<PlayerList players={activeMatch.match_players} />` to `<PlayerList players={activeMatch.match_players ?? []} />`.
       - In the queued section: change `<PlayerList players={queuedMatch.match_players} />` to `<PlayerList players={queuedMatch.match_players ?? []} />`.
       - In `PlayerList` component, add early return: `if (!players || players.length === 0) return null;`.

    Do NOT modify MatchTimer or CompleteMatchButton -- they work correctly.
  </action>
  <verify>
    - `npm run build` passes
    - Visit /admin/courts -- courts with active matches show "Playing: Player1 vs Player2" and "Force End" button
    - Click "Force End" on an occupied court -- confirm dialog appears, court becomes available after confirming
    - Visit /board -- court status board renders without crashes even when match_players is null/empty
    - Open /board in two tabs, end a match in one -- the other tab updates in real-time
  </verify>
  <done>
    Admin can force-end matches from courts page (ADMIN-05 complete). Court status board handles null match_players gracefully. Realtime subscription includes match_players table changes. All MATCH and ADMIN requirements for Phase 5 Plan 01 are satisfied.
  </done>
</task>

</tasks>

<verification>
Phase 5 Plan 01 complete when:
1. `npm run build` succeeds
2. /admin/venue has working sports management (add, remove, save, persist)
3. /admin/courts uses venue sports in dropdown (not hardcoded)
4. /admin/courts shows active match player names on occupied courts
5. /admin/courts "Force End" button works (frees court, returns players to available)
6. /board court status board updates in real-time without null crashes
7. Requirements covered: ADMIN-02, ADMIN-03, ADMIN-05, MATCH-02 through MATCH-05
</verification>

<success_criteria>
- ADMIN-03: Admin can add/remove sports from venue settings, persisted to DB
- ADMIN-05: Admin can force-end active matches from courts admin page
- ADMIN-02: Court CRUD uses dynamic sports from venue
- MATCH-02 through MATCH-05: Verified working (existing code, no changes needed to core logic)
- Court status board is null-safe and subscribes to match_players changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-court-management-admin/05-01-SUMMARY.md`
</output>
