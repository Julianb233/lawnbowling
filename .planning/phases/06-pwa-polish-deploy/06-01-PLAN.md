---
phase: 06-pwa-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/manifest.json
  - src/app/sw.ts
  - src/app/layout.tsx
  - src/components/pwa/InstallPrompt.tsx
  - src/components/pwa/IOSInstallGuide.tsx
  - src/app/offline/page.tsx
  - public/icons/icon-180.png
  - public/icons/icon-152.png
  - public/splash/splash-ipad-landscape.png
  - public/splash/splash-iphone.png
autonomous: true

must_haves:
  truths:
    - "App can be added to home screen on iPad and iPhone and launches in standalone mode"
    - "Service worker caches pages and assets for offline browsing"
    - "Offline fallback page displays when navigating without network"
    - "iOS users see a manual install guide since beforeinstallprompt is not supported"
  artifacts:
    - path: "public/manifest.json"
      provides: "PWA manifest with Apple-specific fields"
      contains: "apple-touch-icon"
    - path: "src/app/sw.ts"
      provides: "Service worker with precaching and runtime caching"
      contains: "runtimeCaching"
    - path: "src/components/pwa/IOSInstallGuide.tsx"
      provides: "iOS-specific install instructions"
      min_lines: 30
    - path: "src/app/offline/page.tsx"
      provides: "Offline fallback page"
      min_lines: 15
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/manifest.json"
      via: "metadata.manifest"
      pattern: "manifest.*manifest.json"
    - from: "src/app/sw.ts"
      to: "/offline"
      via: "fallback navigation"
      pattern: "url.*offline"
    - from: "src/app/layout.tsx"
      to: "src/components/pwa/InstallPrompt.tsx"
      via: "component import"
      pattern: "InstallPrompt"
---

<objective>
Complete PWA setup: manifest polish for iOS/iPadOS installability, service worker caching strategy, iOS install guide, and offline support.

Purpose: The app is used at sports venues on iPads (kiosk) and iPhones (personal). It MUST be installable via "Add to Home Screen" on both devices and handle intermittent connectivity gracefully.

Output: Fully installable PWA with service worker caching, iOS-specific install guidance, and offline fallback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key existing files to read before starting:
@next.config.ts (already has @serwist/next configured)
@src/app/sw.ts (service worker with precache + runtime cache + offline fallback)
@public/manifest.json (basic manifest with 3 icons)
@src/components/pwa/InstallPrompt.tsx (beforeinstallprompt-based, works on Chrome/Edge only)
@src/app/layout.tsx (already has appleWebApp metadata, apple-touch-icon link)
@src/app/offline/page.tsx (basic offline page exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance manifest.json and add Apple-specific PWA meta tags</name>
  <files>
    public/manifest.json
    src/app/layout.tsx
    public/icons/icon-152.png
    public/icons/icon-180.png
    public/splash/splash-ipad-landscape.png
    public/splash/splash-iphone.png
  </files>
  <action>
  1. Update `public/manifest.json`:
     - Add `"scope": "/"` field
     - Add `"prefer_related_applications": false`
     - Add a 180x180 icon entry (Apple standard) and a 152x152 icon entry (iPad)
     - Verify `"display": "standalone"` and `"orientation": "any"` are present (they are)
     - Add `"screenshots"` array (empty is fine, or add placeholders if available)

  2. Generate missing icon sizes using the canvas approach or sharp:
     - Create `public/icons/icon-152.png` (152x152, solid #22c55e background with white "P" letter centered) by copying and resizing from icon-192.png using `npx sharp-cli` or Node canvas script
     - Create `public/icons/icon-180.png` (180x180, same style)
     - If sharp-cli is not available, create a small Node script that uses canvas or just copy icon-192.png as placeholders at those sizes

  3. Generate Apple splash screens:
     - Create `public/splash/` directory
     - Generate splash-iphone.png (1170x2532, dark #0f172a bg with centered "Pick a Partner" text in #22c55e)
     - Generate splash-ipad-landscape.png (2732x2048, same style)
     - Use a simple Node script with canvas or use placeholder solid-color PNGs

  4. Update `src/app/layout.tsx` head section:
     - Add apple-touch-icon links for 152x152 and 180x180 sizes (180 already implied, make explicit)
     - Add apple-mobile-web-app-capable meta via Next.js metadata (already has appleWebApp.capable = true, verify)
     - Add apple-touch-startup-image links for splash screens with media queries:
       ```
       <link rel="apple-touch-startup-image" href="/splash/splash-iphone.png" media="(device-width: 390px)" />
       <link rel="apple-touch-startup-image" href="/splash/splash-ipad-landscape.png" media="(min-device-width: 1024px) and (orientation: landscape)" />
       ```
     - Keep existing metadata exports (Metadata and Viewport) as-is, only add new head children

  AVOID: Do not use next-pwa (the project uses @serwist/next which is already configured). Do not change the Serwist config in next.config.ts.
  </action>
  <verify>
  - `cat public/manifest.json | python3 -m json.tool` validates as JSON
  - `ls public/icons/` shows icon-152.png, icon-180.png, icon-192.png, icon-512.png, icon-maskable-512.png
  - `ls public/splash/` shows splash screen files
  - `grep -n "apple-touch-icon" src/app/layout.tsx` shows multiple icon link tags
  - `grep -n "apple-touch-startup-image" src/app/layout.tsx` shows splash screen links
  </verify>
  <done>
  manifest.json has scope, all required icon sizes, and Apple-compatible fields. Layout has apple-touch-icon links for 152 and 180 sizes plus splash screen startup images. Icon files exist in public/icons/ at all required sizes.
  </done>
</task>

<task type="auto">
  <name>Task 2: iOS install guide component and service worker verification</name>
  <files>
    src/components/pwa/IOSInstallGuide.tsx
    src/components/pwa/InstallPrompt.tsx
    src/app/layout.tsx
    src/app/sw.ts
    src/app/offline/page.tsx
  </files>
  <action>
  1. Create `src/components/pwa/IOSInstallGuide.tsx`:
     - iOS Safari does NOT fire `beforeinstallprompt` - the existing InstallPrompt.tsx only works on Chrome/Android
     - Create a new component that detects iOS Safari (check navigator.userAgent for iPhone/iPad + !navigator.standalone)
     - Show a floating bottom banner (similar style to InstallPrompt) with:
       - Text: "Install Pick a Partner"
       - Step-by-step: "Tap the Share button (box with arrow), then 'Add to Home Screen'"
       - Include a small inline SVG of the iOS share icon (box with up arrow)
       - "Got it" dismiss button that sets localStorage `ios-install-dismissed` to suppress for 7 days
     - Do NOT show if already in standalone mode (navigator.standalone === true or window.matchMedia('(display-mode: standalone)').matches)
     - Use the same glass + green accent design as existing components
     - All interactive elements: min-h-[44px] min-w-[44px]
     - Use `"use client"` directive

  2. Update `src/components/pwa/InstallPrompt.tsx`:
     - Add a check: if iOS detected, return null (let IOSInstallGuide handle it)
     - iOS detection: /iPad|iPhone|iPod/.test(navigator.userAgent) && !('MSStream' in window)
     - No other changes needed - the existing component works well for Chrome/Android

  3. Update `src/app/layout.tsx`:
     - Import and render `<IOSInstallGuide />` alongside `<InstallPrompt />`

  4. Verify `src/app/sw.ts` service worker:
     - READ the existing sw.ts - it already has precacheEntries, skipWaiting, clientsClaim, navigationPreload, runtimeCaching (defaultCache from @serwist/next), and offline fallback
     - Add a custom runtime caching rule for Supabase API calls: NetworkFirst strategy with 5-second timeout for `/api/*` routes so API responses are cached as fallback
     - Add CacheFirst strategy for static assets (images, fonts) with 30-day max age
     - Keep the existing `defaultCache` as the base, spread custom rules BEFORE it so custom rules take priority:
       ```typescript
       runtimeCaching: [
         {
           urlPattern: /^\/api\/.*/i,
           handler: "NetworkFirst",
           options: { cacheName: "api-cache", networkTimeoutSeconds: 5 },
         },
         {
           urlPattern: /\.(?:png|jpg|jpeg|svg|gif|webp|ico)$/i,
           handler: "CacheFirst",
           options: {
             cacheName: "image-cache",
             expiration: { maxEntries: 100, maxAgeSeconds: 30 * 24 * 60 * 60 },
           },
         },
         ...defaultCache,
       ],
       ```

  5. Polish `src/app/offline/page.tsx`:
     - Add a "You're offline" status indicator that auto-detects when connection returns (listen for `online` event)
     - When online event fires, show "You're back online!" message with auto-redirect after 2 seconds
     - Keep existing design, just add the online detection behavior

  AVOID: Do not modify next.config.ts. Do not install additional PWA packages. The @serwist/next setup is correct.
  </action>
  <verify>
  - `cat src/components/pwa/IOSInstallGuide.tsx` exists and contains iOS detection logic
  - `grep "IOSInstallGuide" src/app/layout.tsx` confirms it's imported and rendered
  - `grep "NetworkFirst" src/app/sw.ts` confirms custom runtime caching rules added
  - `grep "CacheFirst" src/app/sw.ts` confirms image caching strategy
  - `grep "online" src/app/offline/page.tsx` confirms online detection
  - `npm run build` succeeds (service worker compiles correctly via Serwist)
  </verify>
  <done>
  iOS users see a native-feeling install guide with share icon instructions. Service worker has optimized caching: NetworkFirst for API, CacheFirst for images, with offline fallback. Offline page auto-detects reconnection. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors (Serwist compiles sw.ts to public/sw.js)
2. `ls public/sw.js` confirms service worker output exists after build
3. `cat public/manifest.json | python3 -m json.tool` is valid JSON with all required fields
4. `ls public/icons/` has all 5 icon files (152, 180, 192, 512, maskable-512)
5. `grep -c "apple-touch" src/app/layout.tsx` returns at least 3 (icons + splash)
6. IOSInstallGuide.tsx contains standalone mode detection
7. sw.ts contains both NetworkFirst and CacheFirst strategies
</verification>

<success_criteria>
- PWA manifest has all required fields for iOS/iPadOS installability
- Icon files exist at 152, 180, 192, and 512px sizes
- Service worker has precaching + custom runtime caching (API, images)
- iOS install guide shows on Safari with share icon instructions
- Chrome/Android install prompt still works via existing InstallPrompt
- Offline page auto-redirects when connection returns
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-pwa-polish-deploy/06-01-SUMMARY.md`
</output>
