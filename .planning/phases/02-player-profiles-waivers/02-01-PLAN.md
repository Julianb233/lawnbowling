# Plan 02-01: Player Profile CRUD & Avatar Upload

## Objective
Implement complete player profile creation, viewing, editing, and avatar upload via Supabase Storage.

## Requirements Covered
- **PROF-01**: User can create a player profile with name and avatar photo
- **PROF-02**: User can set skill level (beginner/intermediate/advanced)
- **PROF-03**: User can select preferred sports (pickleball, lawn bowling, tennis, etc.)
- **PROF-04**: User can view other players' profiles
- **PROF-05**: User can edit their own profile

## Existing Code Inventory

### Already Built
| File | Status | Notes |
|------|--------|-------|
| `src/lib/db/players.ts` | Complete | PlayerProfile type, CRUD functions (getPlayerByUserId, getPlayerById, createPlayer, updatePlayer, uploadAvatar) |
| `src/app/api/profile/route.ts` | Complete | POST (create) + PATCH (update) with auth, validation |
| `src/app/api/profile/avatar/route.ts` | Complete | POST with file type/size validation (5MB limit), Supabase Storage upload |
| `src/app/profile/page.tsx` | Complete | Server component, auth guard, redirects to /profile/setup if no player |
| `src/app/profile/ProfilePageClient.tsx` | Complete | Edit toggle, update via PATCH /api/profile, avatar upload |
| `src/app/profile/setup/page.tsx` | Complete | Server component, redirects to /profile if player exists |
| `src/app/profile/setup/SetupFlowClient.tsx` | Complete | 3-step flow: Profile -> Waiver -> Insurance -> Complete |
| `src/app/profile/[id]/page.tsx` | Complete | Public player profile view with avatar, skill badge, sports tags, insurance status |
| `src/components/profile/ProfileForm.tsx` | Complete | Form with name, skill level (Radix Select), sports selection, avatar upload |
| `src/components/profile/ProfileCard.tsx` | Complete | Card display with avatar, name, skill badge, sports tags, insurance icon |
| `src/components/profile/AvatarUpload.tsx` | Complete | Avatar upload component with preview |
| `src/components/profile/SkillBadge.tsx` | Complete | Skill level badge display |
| `src/components/profile/SportsTags.tsx` | Complete | Sports tag display + SportsSelect multi-picker |
| `src/components/profile/index.ts` | Complete | Barrel exports |

### Gaps / Missing
| Item | Priority | Description |
|------|----------|-------------|
| Input validation | Low | Profile API validates display_name but doesn't sanitize skill_level against valid enum values |
| Supabase Storage bucket | Verify | `avatars` bucket must exist in Supabase with public access |
| RLS policies | Verify | Players table needs RLS: users can read all, write only own rows |

## Tasks

### Task 1: Verify Profile CRUD End-to-End
**Status**: Verification only (code exists)
- [ ] Confirm `players` table exists in Supabase with correct columns: id, user_id, display_name, avatar_url, skill_level, sports, insurance_status, created_at, updated_at
- [ ] Confirm `avatars` storage bucket exists with public read access
- [ ] Confirm RLS policies allow: authenticated users read all players, users write/update only their own rows
- [ ] Test POST /api/profile creates a player and returns it
- [ ] Test PATCH /api/profile updates fields correctly
- [ ] Test POST /api/profile/avatar uploads and returns public URL

### Task 2: Verify Profile Views
**Status**: Verification only (code exists)
- [ ] `/profile` displays current user's profile with edit button
- [ ] `/profile/setup` shows setup flow for new users (redirects existing users)
- [ ] `/profile/[id]` shows any player's public profile
- [ ] ProfileForm validates: name required, at least 1 sport required
- [ ] AvatarUpload shows preview and handles upload

### Task 3: Fix Minor Gaps (if any)
- [ ] Add server-side skill_level enum validation in POST /api/profile if missing
- [ ] Ensure sports array validation accepts only known Sport values
- [ ] Verify Supabase schema migration includes all needed columns

## Estimated Effort
- **If all code works**: ~30 min (verification + minor fixes)
- **If storage/RLS issues**: ~1-2 hours

## Success Criteria
1. New user can create profile with name, avatar, skill level, and sports
2. User can view their own profile at /profile
3. User can edit their profile (name, skill, sports, avatar)
4. Any user can view another player's profile at /profile/[id]
5. Avatar uploads to Supabase Storage and displays correctly
