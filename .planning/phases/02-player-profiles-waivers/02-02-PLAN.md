# Plan 02-02: Liability Waiver Flow, Admin View & DEI Integration

## Objective
Complete the liability waiver signing flow during registration, admin waiver management view, and Daily Event Insurance integration link.

## Requirements Covered
- **WAIV-01**: New users must sign a digital liability waiver during registration before they can play
- **WAIV-02**: Waiver includes checkbox acceptance + timestamp + IP logging
- **WAIV-03**: Waiver acceptance is stored in the database with audit trail
- **WAIV-04**: Admin can view all signed waivers with date/time/player info
- **WAIV-05**: Waiver text is configurable per venue by admin
- **INSR-01**: After waiver signing, users are offered optional event insurance via Daily Event Insurance
- **INSR-02**: DEI microsite check-in flow is embedded/linked for insurance purchase
- **INSR-03**: Insurance status is visible on the player's profile (insured/not insured)

## Existing Code Inventory

### Already Built
| File | Status | Notes |
|------|--------|-------|
| `src/lib/db/waivers.ts` | Complete | Waiver type, createWaiver, getWaiverByPlayerId, listWaivers (with search/pagination) |
| `src/app/api/waiver/sign/route.ts` | Complete | POST with auth, player ownership check, IP + User-Agent capture, creates waiver record |
| `src/app/api/waiver/status/[playerId]/route.ts` | Exists | Waiver status check endpoint |
| `src/app/api/admin/waivers/route.ts` | Exists | Admin waiver list endpoint |
| `src/components/waiver/WaiverForm.tsx` | Complete | Scrollable waiver text, Radix checkbox, submit button, error handling, configurable venueName |
| `src/components/waiver/WaiverStatus.tsx` | Complete | Green (signed) / Yellow (not signed) status badge with date |
| `src/components/waiver/index.ts` | Complete | Barrel exports |
| `src/components/insurance/InsuranceOffer.tsx` | Complete | DEI offer card with external link to dailyeventinsurance.com, dismiss button, status update callback |
| `src/components/insurance/index.ts` | Complete | Barrel exports |
| `src/app/api/insurance/status/route.ts` | Complete | POST to update insurance_status on player (none/active/expired) |
| `src/app/profile/setup/SetupFlowClient.tsx` | Complete | 3-step flow: Profile -> Waiver -> Insurance -> Complete redirect |
| `src/app/admin/waivers/page.tsx` | Complete | Server component with auth + admin role check |
| `src/app/admin/waivers/AdminWaiversClient.tsx` | Complete | Table view with search, player name, signed date, IP, user agent |

### Insurance Status Display (INSR-03)
| File | Shows Insurance? | Notes |
|------|-----------------|-------|
| `src/components/profile/ProfileCard.tsx` | Yes | ShieldCheck (green) if active, Shield (gray) if not |
| `src/app/profile/[id]/page.tsx` | Yes | ShieldCheck/Shield icon next to player name |

### Gaps / Missing
| Item | Priority | Description |
|------|----------|-------------|
| WAIV-05: Configurable waiver text | Medium | WaiverForm accepts `waiverText` prop but there's no admin UI to edit it. Currently hardcoded in both WaiverForm.tsx and waiver/sign/route.ts |
| Waiver gate on board access | High | WAIV-01 says users must sign waiver *before* accessing the availability board. Need middleware or page guard to enforce this |
| Venue-specific waiver storage | Medium | Need a `venue_settings` or `venues` table field for custom waiver text that the API reads |

## Tasks

### Task 1: Verify Waiver Signing Flow
**Status**: Verification only (code exists)
- [ ] Confirm `waivers` table exists with columns: id, player_id, waiver_text, accepted, ip_address, user_agent, signed_at, created_at
- [ ] Confirm RLS: authenticated users can insert own waivers, admins can read all
- [ ] Test the setup flow: profile creation -> waiver signing -> insurance offer -> redirect to board
- [ ] Verify IP address capture works (x-forwarded-for header from Vercel)
- [ ] Verify timestamp is stored correctly in signed_at

### Task 2: Add Waiver Gate for Board Access
**Status**: New code needed
- [ ] Add waiver check to the main board page (/) — if user has no signed waiver, redirect to /profile/setup or show waiver-required message
- [ ] Consider adding to middleware or as a layout-level check for all authenticated routes
- [ ] Players without a signed waiver should only access: /profile/setup, /profile, /login, /signup

### Task 3: Add Admin Waiver Text Configuration (WAIV-05)
**Status**: New code needed
- [ ] Add `waiver_text` column to `venues` table (or create a `venue_settings` table)
- [ ] Create admin UI at /admin/venue or /admin/waivers to edit the waiver text
- [ ] Update `POST /api/waiver/sign` to read waiver text from venue settings instead of hardcoded constant
- [ ] Update `WaiverForm` to receive venue-specific waiver text from the server

### Task 4: Verify Admin Waiver View
**Status**: Verification only (code exists)
- [ ] `/admin/waivers` displays all signed waivers in a table
- [ ] Table shows: player name (linked), signed date, IP address, user agent
- [ ] Search filters by player name
- [ ] Only admin users can access the page (role check in server component)

### Task 5: Verify DEI Insurance Integration
**Status**: Verification only (code exists)
- [ ] Insurance offer appears after waiver signing in setup flow
- [ ] "Get Coverage" button links to https://dailyeventinsurance.com
- [ ] Clicking the link triggers onStatusUpdate("active") to update insurance_status
- [ ] "Maybe Later" dismisses the offer and continues to board
- [ ] Insurance status icon (ShieldCheck/Shield) shows on ProfileCard and player profile page

## Estimated Effort
- **Verification of existing code**: ~30 min
- **Waiver gate (Task 2)**: ~1 hour
- **Admin waiver config (Task 3)**: ~2 hours
- **Total**: ~3-4 hours

## Success Criteria
1. New user must complete waiver during registration before reaching the board (WAIV-01)
2. Waiver record includes checkbox acceptance, timestamp, and IP address (WAIV-02, WAIV-03)
3. Admin can view all signed waivers with player info, date, IP (WAIV-04)
4. Waiver text can be configured by admin per venue (WAIV-05)
5. After waiver, user sees optional DEI insurance link (INSR-01, INSR-02)
6. Insurance status is visible on player profiles (INSR-03)

## Dependencies
- Plan 02-01 must be complete (player profiles must exist before waivers reference them)
- Supabase database schema must include waivers table and venues table
