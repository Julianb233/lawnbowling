---
phase: 02-player-profiles-waivers
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/app/api/waiver/sign/route.ts
  - src/app/api/waiver/status/[playerId]/route.ts
  - src/app/api/admin/waivers/route.ts
  - src/app/api/insurance/status/route.ts
  - src/app/admin/waivers/page.tsx
  - src/app/admin/waivers/AdminWaiversClient.tsx
  - src/app/admin/venue/page.tsx
  - src/lib/db/waivers.ts
  - src/lib/db/venues.ts
  - src/components/waiver/WaiverForm.tsx
  - src/components/waiver/WaiverStatus.tsx
  - src/components/insurance/InsuranceOffer.tsx
autonomous: true

must_haves:
  truths:
    - "User must sign liability waiver before accessing the availability board"
    - "Waiver acceptance is logged with timestamp, IP address, and user agent"
    - "After waiver signing, user is offered optional Daily Event Insurance link"
    - "Admin can view all signed waivers with player name, date, IP"
    - "Admin can configure the waiver text per venue"
    - "Insurance status (insured/not insured) is visible on player profiles"
  artifacts:
    - path: "src/app/api/waiver/sign/route.ts"
      provides: "POST endpoint to sign waiver with IP/UA capture"
      exports: ["POST"]
    - path: "src/app/api/admin/waivers/route.ts"
      provides: "GET endpoint listing all signed waivers (admin only)"
      exports: ["GET"]
    - path: "src/app/admin/waivers/AdminWaiversClient.tsx"
      provides: "Admin waiver list table with search"
    - path: "src/lib/db/waivers.ts"
      provides: "Waiver CRUD functions"
      exports: ["createWaiver", "getWaiverByPlayerId", "listWaivers"]
    - path: "src/lib/db/venues.ts"
      provides: "Venue settings including waiver text"
      exports: ["getVenueSettings", "updateVenueSettings"]
    - path: "src/components/insurance/InsuranceOffer.tsx"
      provides: "DEI offer card with link to dailyeventinsurance.com"
  key_links:
    - from: "src/app/profile/setup/SetupFlowClient.tsx"
      to: "/api/waiver/sign"
      via: "fetch POST after waiver checkbox acceptance"
      pattern: "fetch.*api/waiver/sign"
    - from: "src/app/profile/setup/SetupFlowClient.tsx"
      to: "/api/insurance/status"
      via: "fetch POST when user clicks Get Coverage"
      pattern: "fetch.*api/insurance/status"
    - from: "src/app/admin/waivers/AdminWaiversClient.tsx"
      to: "/api/admin/waivers"
      via: "fetch GET on mount"
      pattern: "fetch.*api/admin/waivers"
    - from: "src/app/api/waiver/sign/route.ts"
      to: "src/lib/db/venues.ts"
      via: "reads venue waiver text instead of hardcoded"
      pattern: "getVenueSettings"
---

<objective>
Complete the liability waiver flow, admin waiver management, configurable waiver text, and Daily Event Insurance integration.

Purpose: Users must sign a liability waiver during registration before they can access the availability board. The waiver acceptance must be auditable (timestamp, IP, user agent). Admin needs visibility into all signed waivers and ability to customize waiver text. After signing, users get an optional DEI insurance offer.

Output: Working waiver signing flow integrated into setup, admin waiver viewer, configurable waiver text, DEI insurance link, and insurance status display on profiles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Key existing files (already scaffolded):
@src/lib/db/waivers.ts
@src/lib/db/venues.ts
@src/app/api/waiver/sign/route.ts
@src/app/api/waiver/status/[playerId]/route.ts
@src/app/api/admin/waivers/route.ts
@src/app/api/insurance/status/route.ts
@src/app/admin/waivers/page.tsx
@src/app/admin/waivers/AdminWaiversClient.tsx
@src/components/waiver/WaiverForm.tsx
@src/components/waiver/WaiverStatus.tsx
@src/components/insurance/InsuranceOffer.tsx
@src/app/profile/setup/SetupFlowClient.tsx
@src/components/profile/ProfileCard.tsx
@src/app/profile/[id]/page.tsx
@src/lib/auth/admin.ts

Prior plan context:
- 02-01 adds middleware gating that checks for `pap_has_waiver` cookie
- 02-01 adds GET /api/profile endpoint
- SetupFlowClient.tsx already has the 3-step flow wired (profile -> waiver -> insurance)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete waiver API, configurable text, and cookie gating</name>
  <files>
    src/app/api/waiver/sign/route.ts
    src/app/api/waiver/status/[playerId]/route.ts
    src/lib/db/waivers.ts
    src/lib/db/venues.ts
    src/app/admin/venue/page.tsx
  </files>
  <action>
    1. **src/app/api/waiver/sign/route.ts** -- Currently complete and functional. Make these improvements:
       - After successful waiver creation, set a `pap_has_waiver=1` cookie on the response (path `/`, maxAge 1 year) so the middleware from Plan 02-01 can skip DB checks for waiver status
       - Replace the hardcoded `DEFAULT_WAIVER_TEXT` with a dynamic lookup: call `getVenueSettings()` from `src/lib/db/venues.ts` to get the venue's configured waiver text. Fall back to the hardcoded default if no venue settings exist.
       - The route already captures IP via `x-forwarded-for` / `x-real-ip` headers and user-agent. This is correct for Vercel deployments.

    2. **src/lib/db/venues.ts** -- Check if this file exists. If it does, read it and add/verify these functions:
       - `getVenueSettings(venueId?: string)` -- returns venue row from `venues` table. If no venueId provided, return the first venue (single-venue MVP). Include a `waiver_text` column. If the venues table doesn't have a `waiver_text` column, add it to the schema expectations and document it.
       - `updateVenueSettings(venueId: string, updates: { waiver_text?: string; name?: string; address?: string })` -- updates venue row
       - If the file doesn't exist, create it with these functions using the same pattern as `src/lib/db/players.ts` (import createClient from server, query supabase)

    3. **src/app/api/waiver/status/[playerId]/route.ts** -- Read and verify:
       - Has a GET handler that returns waiver status for a given player ID
       - Returns the waiver object or null
       - Should be accessible by any authenticated user (for checking other players' waiver status on profile pages)
       If incomplete, implement: call `getWaiverByPlayerId(playerId)`, return JSON result

    4. **src/app/admin/venue/page.tsx** -- Read existing file. Add waiver text configuration:
       - This page likely exists for venue name/address editing
       - Add a "Waiver Text" section with a `<textarea>` for editing the waiver content
       - On save, POST/PATCH to an admin venue API endpoint that updates `waiver_text` on the venues table
       - If no admin venue API exists at `src/app/api/admin/venue/route.ts`, read it and add PATCH support for `waiver_text` field
       - Use the existing admin auth pattern: check `isAdmin(user.id)` or `user.app_metadata?.role === "admin"`

    5. **src/lib/db/waivers.ts** -- Already complete with createWaiver, getWaiverByPlayerId, listWaivers. No changes needed. The `listWaivers` function already joins `players(display_name, avatar_url)` which is exactly what the admin view needs.
  </action>
  <verify>
    - `npm run build` passes
    - POST /api/waiver/sign creates waiver record with IP, user agent, timestamp, and sets pap_has_waiver cookie
    - GET /api/waiver/status/[playerId] returns waiver or null
    - Venue settings can store custom waiver text
  </verify>
  <done>
    - Waiver signing captures timestamp, IP address, and user agent (WAIV-02, WAIV-03)
    - Waiver record stored in database with full audit trail (WAIV-03)
    - pap_has_waiver cookie set after signing for middleware performance (WAIV-01 enforcement)
    - Waiver text is read from venue settings, falls back to default (WAIV-05)
    - Admin can edit waiver text via /admin/venue page (WAIV-05)
    - Waiver status check endpoint works for any player (WAIV-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete admin waiver view and DEI insurance integration</name>
  <files>
    src/app/admin/waivers/page.tsx
    src/app/admin/waivers/AdminWaiversClient.tsx
    src/app/api/admin/waivers/route.ts
    src/app/api/insurance/status/route.ts
    src/components/insurance/InsuranceOffer.tsx
    src/components/waiver/WaiverStatus.tsx
  </files>
  <action>
    1. **src/app/admin/waivers/AdminWaiversClient.tsx** -- Already complete with:
       - Table showing player name (linked to /profile/[id]), signed date, IP address, user agent
       - Search filtering by player name (client-side)
       - Loading state with spinner
       - Empty state message
       Verify it works. One improvement: add pagination. Currently fetches all waivers at once. Add a "Load More" button if `total > waivers.length`:
       - Track `offset` in state, start at 0
       - "Load More" button calls `fetchWaivers(offset + limit)` and appends results
       - Show "Showing X of Y waivers" text

    2. **src/app/admin/waivers/page.tsx** -- Already complete with auth + admin check. The admin check uses `user.app_metadata?.role === "admin" || user.user_metadata?.role === "admin"`. This is different from `isAdmin()` in admin.ts which checks the `players` table role column. Standardize: use `isAdmin(user.id)` from `@/lib/auth/admin` for consistency. Import and use it.

    3. **src/app/api/admin/waivers/route.ts** -- Already complete. Uses `isAdmin()` check, calls `listWaivers()` with pagination params. No changes needed.

    4. **src/app/api/insurance/status/route.ts** -- Already complete. POST endpoint accepts `{ status: "none" | "active" | "expired" }`, validates, updates player record. No changes needed.

    5. **src/components/insurance/InsuranceOffer.tsx** -- Already complete:
       - Links to https://dailyeventinsurance.com in new tab (INSR-02)
       - Calls `onStatusUpdate("active")` when "Get Coverage" is clicked (INSR-03)
       - "Maybe Later" button dismisses and calls `onDismiss()` (INSR-01)
       No changes needed.

    6. **src/components/waiver/WaiverStatus.tsx** -- Already complete:
       - Shows green "Waiver Signed" with date if waiver exists
       - Shows yellow "Waiver Not Signed" warning if null
       No changes needed.

    7. **Insurance status display verification** -- Already wired in:
       - `src/components/profile/ProfileCard.tsx` shows ShieldCheck (green) for active, Shield (gray) for not (INSR-03)
       - `src/app/profile/[id]/page.tsx` shows same icons next to player name (INSR-03)
       No changes needed.
  </action>
  <verify>
    - `npm run build` passes
    - /admin/waivers shows table of all signed waivers with player names, dates, IPs
    - /admin/waivers is only accessible by admin users (non-admin gets redirected)
    - InsuranceOffer component renders correctly in setup flow step 3
    - Insurance status icons show on ProfileCard and public profile pages
  </verify>
  <done>
    - Admin can view all signed waivers with player name, date, IP, user agent (WAIV-04)
    - Admin waiver view has search and pagination (WAIV-04)
    - Admin auth check uses consistent isAdmin() function
    - After waiver, user sees DEI insurance offer with link to dailyeventinsurance.com (INSR-01, INSR-02)
    - Insurance status visible on player profiles via shield icons (INSR-03)
    - "Maybe Later" dismisses offer and continues to board
  </done>
</task>

</tasks>

<verification>
Requirements covered by this plan:
- WAIV-01: Digital liability waiver required during registration before playing
- WAIV-02: Checkbox acceptance + timestamp + IP logging
- WAIV-03: Waiver stored in database with audit trail
- WAIV-04: Admin can view all signed waivers with date/time/player info
- WAIV-05: Waiver text configurable per venue by admin
- INSR-01: After waiver, user offered optional DEI insurance
- INSR-02: DEI microsite linked for insurance purchase
- INSR-03: Insurance status visible on player profile

Run:
```bash
cd /opt/agency-workspace/pick-a-partner && npm run build
```

End-to-end flow test:
1. Sign up -> redirected to /profile/setup
2. Fill profile (step 1) -> waiver appears (step 2)
3. Read waiver, check checkbox, submit -> insurance offer (step 3)
4. Click "Get Coverage" (opens DEI site) or "Maybe Later" -> redirect to /board
5. Visit /profile -> waiver status shows "Signed" with date
6. Admin visits /admin/waivers -> sees all signed waivers in table
</verification>

<success_criteria>
- New user cannot access /board without signing waiver (middleware + cookie gating)
- Waiver record includes accepted flag, signed_at timestamp, ip_address, user_agent
- Admin at /admin/waivers sees searchable table of all signed waivers
- Admin can edit waiver text at /admin/venue
- InsuranceOffer component appears after waiver step with DEI link
- Insurance status icons (ShieldCheck/Shield) appear on ProfileCard and /profile/[id]
</success_criteria>

<output>
After completion, create `.planning/phases/02-player-profiles-waivers/02-02-SUMMARY.md`
</output>
